# Aurora Database
aurora_endpoint: localhost
aurora_port: 5432
aurora_database_name: giki_test

# Legacy (v1) database — not connected in CI (the migration's Source layer is
# stubbed in tests); present only so Giki.config.aurora_legacy_database_name resolves.
aurora_legacy_database_name: giki_legacy_test

# Frontend URL for emails and CORS
frontend_base_url: http://localhost:3211

# Admin URL for CORS
admin_base_url: http://localhost:3012

# API URL, used to build absolute URLs to API-served assets
api_base_url: http://localhost:3210

# Cloudflare R2 (test values)
r2_account_id: "test-account-id"
r2_endpoint: "http://localhost:4566"
r2_bucket_assets: "test-assets"
r2_bucket_uploads: "test-uploads"
r2_region: "us-east-1"
assets_cdn_url: "https://test-assets.example.com"
uploads_host: ""

# Stripe Configuration (test mode)
stripe_publishable_key: pk_test_fake_publishable_key
stripe_premium_monthly_price_id: price_premium_monthly_test
stripe_premium_annual_price_id: price_premium_annual_test

# Email Configuration
mail_from_email: noreply@gikiactions.com
marketing_from_email: hello@gikiactions.com
notifications_from_email: notifications@gikiactions.com
support_email: info@gikiactions.com

# SES (legacy — kept for Giki.ses_client in lib/giki.rb)
ses_region: eu-west-1

# Sentry Error Tracking (disabled in test)
sentry_dsn: ""

# Legacy API
legacy_api_base_url: https://api.gikiactions.com

# Cloudflare Browser Rendering
cloudflare_account_id: "0a0e6f92decf825364b860e2286ceebf"

# Airtable
airtable_base_id: "appJHsHtDQoVjwBe0"
airtable_table_id: "tblYUXFx9JiIxToIV"
airtable_downloadables_table_id: "tblq6eQAum9Bmiz0v"
airtable_industries_table_id: "tblwxJnrctw0edVjN"
