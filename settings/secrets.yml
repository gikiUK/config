# Aurora Database Credentials
aurora_username: "giki_user"
aurora_password: "giki_password"

# v1 (legacy) ActiveRecord::Encryption keys — used ONLY by the v1→v2 migration
# to decrypt v1's deterministic `email` column. Not real values here; the real
# v1 keys live in AWS Secrets Manager in production. Placeholder length matches a
# valid 32-byte key so ActiveRecord::Encryption configures cleanly at boot.
v1_encryption_primary_key: "0000000000000000000000000000000000000000000a"
v1_encryption_deterministic_key: "0000000000000000000000000000000000000000000b"
v1_encryption_key_derivation_salt: "0000000000000000000000000000000000000000000c"

# Job Monitor UI Credentials (Solid Queue Monitor)
job_monitor_username: "admin"
job_monitor_password: "admin"

# Legacy API
legacy_api_bearer_token: "placeholder"

# Cloudflare Browser Rendering
cloudflare_pdf_api_key: "placeholder"

# Cloudflare R2 (fake credentials for LocalStack in dev/test)
r2_access_key_id: "fake-r2-access-key-id"
r2_secret_access_key: "fake-r2-secret-access-key"

# Rails secret key base (required in production)
secret_key_base: "dev-secret-key-base-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Airtable
airtable_api_key: "placeholder"

# Gemini (Google AI)
gemini_api_key: "placeholder"

# Stripe
stripe_secret_key: "sk_test_placeholder"
stripe_webhook_signing_secret: "whsec_placeholder"

# SendGrid
sendgrid_api_key: "SG.placeholder"
